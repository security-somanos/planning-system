# Environment
ENV=dev

# Server
PORT=8080

# Database (from Docker container)
# Container exposes port 5423 on host -> 5432 in container
DATABASE_URL=postgres://planning:planning123@localhost:5423/planning_system?sslmode=disable

# Logging
LOG_LEVEL=info

# Migrations & Seeding
AUTO_MIGRATE=true
AUTO_SEED=true

# CORS (optional - defaults to common local dev ports if not set)
# CORS_ORIGINS=http://localhost:3000,http://localhost:51731